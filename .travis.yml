language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - '$HOME/.npm'
script:
  - npm ci
  - npm test
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $githubToken
  local_dir: build
  on:
    branch: master
